{
    "private": true,
    "name": "mono",
    "version": "0.0.20",
    "scripts": {
        "clean": "git clean -fdX",
        "build": "pnpm -r --filter \"./packages/vorplex/**\" build",
        "test": "pnpm -r --filter \"./packages/vorplex/**\" test",
        "version": "npx tsx .github/scripts/version.ts",
        "package-list": "npx tsx .github/scripts/package-list.mts"
    },
    "devDependencies": {
        "@types/node": "workspace:*",
        "@vorplex/core": "workspace:*",
        "@vorplex/node": "workspace:*",
        "tsx": "workspace:*",
        "vitest": "workspace:*",
        "yaml": "workspace:*"
    }
}
